/**
 * Global configuration flags
 */

// Set to true to use local test data instead of API
export const USE_LOCAL_TEST_DATA = true;

// Maximum distance in miles to show locations (default: 200 miles)
export const MAX_LOCATION_DISTANCE_MILES = 200;

